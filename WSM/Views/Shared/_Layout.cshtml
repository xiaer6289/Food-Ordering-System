<!DOCTYPE html>
<html lang="en">
 <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/css/app.css" asp-append-version="true">
    @RenderSection("head", false) @*optional section name head for individual page to inject content*@
    <script src="~/js/app.js"></script>

 </head>
    <body>
    <header>
        @* <nav class="nav-bar">
            <a class="title">Restaurant Ordering System</a>

            <form class="searchBar" asp-controller="Search" asp-action="Global" method="get">
                <input type="search" id="search" name="q" placeholder="@ViewBag.SearchPlaceholder" />
                <input type="hidden" name="context" value="@ViewBag.SearchContext" />
                <button type="submit">Search</button>
            </form>

            


             <div class="button-group">
                <a class="orderLogout" href="home/menu">Order</a>
                <button class="orderLogout" onclick="window.location.href='@Url.Action("Login", "Authorization")'">Log Out </button>
                    

            </div> 
        </nav> *@
        <div class="left-header">
            <div class="logo">
                <img src="@(
                     string.IsNullOrEmpty(Context.Session.GetString("CompanyLogo"))
                         ? Url.Content("~/images/default-logo.png")
                         : Url.Content(Context.Session.GetString("CompanyLogo"))
        )" alt="Company Logo" style="max-height:50px; " />
            </div>



            <form class="search" asp-controller="Search" asp-action="Global" method="get">
                <i>🔎</i>
                <input type="search" id="search" name="q" placeholder="@ViewBag.SearchPlaceholder" />
                <input type="hidden" name="context" value="@ViewBag.SearchContext" />
            </form>

        </div>

        <nav class="header-navigation">
            <a href="#">Home</a>
            <a href="../Order/Cart.cshtml">Order</a>
            <a href="../Food/Menu.cshtml">Menu</a>
            <a href="#">...</a>
        </nav>

        <div class="potrait" onclick="ToggleEventDropdown()">
            <img src="...\images\BeePos.png" alt="User Potrait" />

            <div class="dropdown" id="dropdownmenu">
                <div class="view">
                    <p><strong>empty</strong></p>
                    <p><strong>empty</strong></p>
                </div>
                <a href="#">View Profile</a>
                <a href="#">Logout</a>
            </div>
        </div>


    </header>


        <main>
            @RenderBody()
        </main>
        <script src="/js/jquery.min.js"></script>
        <script src="/js/jquery.unobtrusive-ajax.min.js"></script>
        <script src="/js/jquery.validate.min.js"></script>
        <script src="/js/jquery.validate.unobtrusive.min.js"></script>
        <script src="/js/app.js" asp-append-version="true"></script>

        @RenderSection("scripts", required: false)
    </body>
</html>

<script>
    function toggleDropdown() {
      document.getElementById("dropdownMenu").classList.toggle("show");
    }

    // Close dropdown if clicked outside
    window.onclick = function(event) {
      if (!event.target.matches('.profile img')) {
        let dropdown = document.getElementById("dropdownMenu");
        if (dropdown.classList.contains('show')) {
          dropdown.classList.remove('show');
        }
      }
    }
</script>